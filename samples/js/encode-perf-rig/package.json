{
  "name": "noms-encode-perf-rig",
  "main": "dist/main.js",
  "scripts": {
    "preinstall": "mkdir -p node_modules/.bin && for n in babel eslint flow; do ln -sf ../../../node_modules/.bin/${n} node_modules/.bin/${n}; done",
    "prepublish": "npm run build",
    "start": "babel -d dist -w src",
    "build": "BABEL_ENV=production babel -d dist src",
    "test": "eslint src/ && flow src/"
  }
}
